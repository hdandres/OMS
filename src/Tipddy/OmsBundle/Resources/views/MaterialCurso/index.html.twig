{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}
<h1>Listado Material de Cursos</h1>

<table class="listado">
    <thead>
        <tr>
            <!--<th>Id</th>-->
            <th>Descripción</th>
            <th>Cantidad</th>
            <th>Tipo Material</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <!--<td><a href="{{ path('coursematerial_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>-->
            <td>{{ entity.descripcion }}</td>
            <td>{{ entity.cantidad }}</td>
            <td>{{ entity.tipoMaterial }}</td>
            <td align="left">
                <ul>
                    <li>
                        <a href="{{ path('coursematerial_show', { 'id': entity.id }) }}">Ver</a>
                        &nbsp;&nbsp;    
                        <a href="{{ path('coursematerial_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('coursematerial_new') }}">
            Nuevo Registro
        </a>
    </li>
</ul>
{% endblock %}
