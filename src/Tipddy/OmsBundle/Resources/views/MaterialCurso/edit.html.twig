{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}

<form action="{{ path('coursematerial_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {#{ form_widget(form) }#}
       <fieldset>
            <legend>Edición Material</legend>
            {% include 'TipddyOmsBundle:MaterialCurso:_form.html.twig' %}
        </fieldset>  
    <p>
        <button type="submit">Editar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('coursematerial') }}">
            Volver a Listado
        </a>
    </li>
    <li>
        <form action="{{ path('coursematerial_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
 
            <button type="submit">Eliminar</button>
        </form>
    </li>
</ul>
{% endblock %}
