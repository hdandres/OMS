{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}

<form action="{{ path('otec_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {#{ form_widget(form) }#}
         <fieldset>
             <legend>Edición Otec</legend>
            {% include 'TipddyOmsBundle:Otec:_form.html.twig' %}
        </fieldset>               
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('otec') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('otec_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}