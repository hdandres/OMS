{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}

<form action="{{ path('participant_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {#{ form_widget(form) }#}
     <fieldset>
	 <legend>Edición Participante</legend>
  	 {% include 'TipddyOmsBundle:Participante:_form.html.twig' %}
     </fieldset>       
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('participant') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('participant_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
