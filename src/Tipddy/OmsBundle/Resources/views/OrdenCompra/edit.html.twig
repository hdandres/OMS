{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}

<form action="{{ path('purchaseorder_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {#{ form_widget(form) }#}
       <fieldset>
             <legend>Edición Orden</legend>
            {% include 'TipddyOmsBundle:OrdenCompra:_form.html.twig' %}
        </fieldset>                
    <p>
        <button type="submit">Editar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('purchaseorder') }}">
            Volver a Listado
        </a>
    </li>
    <li>
        <form action="{{ path('purchaseorder_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
 
            <button type="submit">Eliminar</button>
        </form>
    </li>
</ul>
{% endblock %}
