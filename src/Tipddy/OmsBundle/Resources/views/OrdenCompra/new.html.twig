{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}
<h1>OrdenCompra creation</h1>

<form action="{{ path('purchaseorder_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('purchaseorder') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
