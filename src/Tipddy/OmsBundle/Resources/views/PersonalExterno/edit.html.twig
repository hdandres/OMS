{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}
<h1>PersonalExterno edit</h1>

<form action="{{ path('outsidestaff_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {#{ form_widget(edit_form) }#}
    <fieldset>
	 <legend>Edición Personal</legend>
  	 {% include 'TipddyOmsBundle:PersonalExterno:_form.html.twig' %}
    </fieldset>        
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('outsidestaff') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('outsidestaff_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
