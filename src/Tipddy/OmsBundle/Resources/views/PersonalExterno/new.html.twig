{% extends 'TipddyOmsBundle::layout.html.twig' %}

{% block body %}

<form action="{{ path('outsidestaff_create') }}" method="post" {{ form_enctype(form) }}>
    {#{ form_widget(form) }#}
    <fieldset>
	 <legend>Nuevo Personal</legend>
  	 {% include 'TipddyOmsBundle:PersonalExterno:_form.html.twig' %}
    </fieldset>

    <p>
        <button type="submit">Crear</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('outsidestaff') }}">
            Volver a Listado
        </a>
    </li>
</ul>
{% endblock %}